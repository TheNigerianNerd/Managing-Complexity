<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- Force NuGet to use a container-local global packages folder to avoid host-specific fallback paths -->
  <config>
    <add key="globalPackagesFolder" value="/root/.nuget/packages" />
  </config>
</configuration>
